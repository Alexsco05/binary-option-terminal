FILE="$HOME/gideon_bridge/inbox.command"

while true; do
  if [ -s "$FILE" ]; then
    echo "Command detected..."

    cmd=$(cat "$FILE")

    echo "Running: $cmd"
    eval "$cmd"

    > "$FILE"
  fi

  sleep 2
done
